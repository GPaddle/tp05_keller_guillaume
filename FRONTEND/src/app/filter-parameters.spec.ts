import { FilterParameters } from './filter-parameters';

describe('FilterParameters', () => {
  it('should create an instance', () => {
    expect(new FilterParameters()).toBeTruthy();
  });
});
